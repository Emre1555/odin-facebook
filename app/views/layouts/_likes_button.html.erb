<% like = current_user.likes.find_by(likable: likable) %>
<% if like %>
  <%= button_to "Unlike", like_path(like), method: :delete %>
<% else %>
    <%= button_to "Like", likes_path(like: { likable_id: likable.id, likable_type: likable.class.to_s }), method: :post %>
<% end %>